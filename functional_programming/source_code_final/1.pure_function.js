const calculateArea = (radius, pi) =>
  radius * radius * pi;

// ------------------------------

const counter = 1;

const increaseCounter = () => counter + 1;

counter; // 1
increaseCounter(); // 2
counter; // 1
